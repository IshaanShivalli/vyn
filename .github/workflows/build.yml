name: Build Vyn

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build compiler
        run: |
          echo "Building Vyn..."
          # Put your compiler build command here